This folder is intentionally lightweight. The portfolio content and project UI are rendered by React from src/.
Static files that must be copied without processing can be placed here.
Do not delete the src/ folder or package.json — they contain the application code.
